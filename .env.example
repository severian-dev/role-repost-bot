# Required: Bot token from Discord Developer Portal
DISCORD_TOKEN=

# Required: Application ID from Discord Developer Portal
CLIENT_ID=

# Optional: Guild ID for development command deployment (faster updates)
# If not set, commands are deployed globally (takes up to 1 hour to propagate)
GUILD_ID=

# Optional: Path to SQLite database file (defaults to ./repost.db)
DATABASE_PATH=
